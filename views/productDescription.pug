extends layout

block content
    div.grid-container
        each product in products
            a.item-title(href="/products/" + product._id)
            div
                strong Prix : #{product.price} €
            hr
            div #{product.description}
        else
            p Produit inexistant
    hr
    p
        each product in products
            a.inline-10(href='/products/' + product._id + '/delete') Supprimer le produit
            a.inline-10(href='/products/' + product._id + '/update') Modifier le produit
